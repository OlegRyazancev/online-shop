spring:
  application:
    name: eureka-server

server:
  port: 8761

#Zipkin
management:
  tracing:
    sampling:
      probability: 1.0

#Eureka
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
